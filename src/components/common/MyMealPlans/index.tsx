import { FilterBlock, Grid } from '@root/components/common';
import { FC } from 'react';

export const MyMealPlans: FC = () => {
    return (
        <section>
            <FilterBlock />
            <Grid />
        </section>
    );
};
