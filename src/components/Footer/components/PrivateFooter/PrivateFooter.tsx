import { FC } from 'react';

import { FooterNavigation } from '@root/components';

export const PrivateFooter: FC = () => {
    return (
        <>
            <FooterNavigation />
        </>
    );
};
