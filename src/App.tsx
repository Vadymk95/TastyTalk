import { Footer, Header, Main } from '@root/components';
import { FC } from 'react';
import './App.css';

export const App: FC = () => {
    return (
        <>
            <Header />
            <Main />
            <Footer />
        </>
    );
};
